# PostgreSQL connection string for Prisma
DATABASE_URL="postgresql://user:password@localhost:5432/noavaranzibayi?schema=public"

# Auth.js — generate with: openssl rand -base64 32
AUTH_SECRET=""

# SMTP (optional — leave unset to no-op email sending in development)
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
MAIL_FROM="Noavaran Zibayi <no-reply@noavaranzibayi.com>"

# First Super Admin — required to run `pnpm db:seed`.
# Use a strong, unique password; change it after first login.
SUPER_ADMIN_NAME="Super Admin"
SUPER_ADMIN_EMAIL=""
SUPER_ADMIN_PASSWORD=""
